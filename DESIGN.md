# تصميم مِعيار

مِعيار تطبيق محادثة جوال، لا لوحة تحكم. كل شاشة وكل حركة تخدم سؤال الطالب في القدرات: كمي، لفظي، محاكاة، وخطة مذاكرة.

## ألوان

| Token | Hex | الاستخدام |
| --- | --- | --- |
| Ink | `#0D0F12` | الخلفية الرئيسية. داكنة راقية وليست سوداء بالكامل. |
| Surface | `#171A1F` | حقل الكتابة، بطاقات الأسئلة، درج المحادثات. |
| Elevated | `#20252B` | فقاعة المستخدم والعناصر فوق السطح. |
| Gold | `#D7B674` | الإرسال، الشعار، التأكيد الهادئ. الهوية الأساسية. |
| Mint | `#8AD6B5` | نجاح الإجابة ولمسة الذكاء الاصطناعي في المؤشر. |
| Text | `#F5F6F7` | النص الأساسي. |
| Muted | `#9CA3AF` | الوصف والنص الثانوي. |

لا يُستخدم البنفسجي كهوية. لا تدرجات لونية مبالغ فيها. الحدود بشفافية بيضاء خفيفة (`rgba(245, 246, 247, 0.08)`) والظلال هادئة فقط حول حقل الكتابة والدرج.

## التباعد

سلم ثابت: `4 / 8 / 12 / 16 / 20 / 24 / 32 / 40`.

- هوامش الشاشة: `16`.
- مسافة الرسائل: `20`.
- حقل الكتابة: حد أدنى `56` مع حشو `8`.
- الشاشات المستهدفة: `375×812` و`390×844`. البطاقات تلتف ولا تُجبر على عمودين ضيقين جدًا.

## الخطوط

خط النظام العربي عبر Expo، بدون ملف خط إضافي.

| أسلوب | الحجم | الارتفاع | الوزن |
| --- | --- | --- | --- |
| Display | 28 | 36 | 700 |
| Title | 17 | 24 | 700 |
| Body | 16 | 26 | 400 |
| Callout | 14 | 22 | 600 |
| Caption | 12 | 18 | 500 |
| Micro | 11 | 16 | 500 |

النص العربي يستخدم `writingDirection: 'rtl'` و`textAlign: 'right'` إلا إذا كان العنصر صفًا يبدأ من جهة البداية المنطقية.

## الحركة

المبدأ: انتقال قصير بين الحالات، بلا مبالغة وبلا قفز للواجهة.

| حالة | السلوك |
| --- | --- |
| التفكير | Orb دائري محلي (حلقة ذهبية بلمسة نعناع) مع النص «مِعيار يفكر...». نبض خفيف ودوران بطيء. |
| الرد التدريجي | ظهور النص كلمةً كلمة بعد التفكير. البطاقات تظهر بعد اكتمال النص حتى لا يقفز التخطيط. |
| تبديل الأيقونات | زر Plus يدور `45°` فيصبح إغلاقًا. الميكروفون يتحول إلى إرسال ذهبي عند وجود نص. |
| درج المحادثات | تعتيم هادئ وانزياح بسيط من اليمين. |
| توسع السؤال | بعد اختيار خيار تظهر خطوات الحل بـ `LayoutAnimation`. نجاح بنعناع وخطأ بوردي خفيف. |
| Reduced motion | إيقاف النبض والدوران والبث التدريجي. يظهر الرد مكتملًا بعد توقف قصير. |

المدد: تبديل `180ms`، أساس `280ms`، درج `340ms`، بث `22ms` لكل مقطع. كل الحركة عبر React Native، بدون WebGL أو مكتبات إضافية.

## قواعد RTL

- النص عربي عبر `writingDirection: 'rtl'` و`textAlign: 'right'`. على الويب يُضبط `document.documentElement.dir = 'rtl'`.
- React Native Web لا يقبل خاصية `direction` على الـ View. لذلك:
  - الويب يرث `dir=rtl` من المستند ويستخدم `flexDirection: 'row'`.
  - iOS/Android يستخدمان `row-reverse` لأن Yoga يبقى LTR ما لم يكن نظام التشغيل عربيًا.
  - هذا موحّد في `src/theme/layout.ts` حتى لا ينقلب الصف مرتين.
- رسائل المستخدم تلتصق باليمين. رسائل مِعيار ومؤشر التفكير على اليسار.
- زر المحادثات على يمين الرأس، وزر محادثة جديدة على اليسار.
- Plus على يمين حقل الكتابة (بداية النص العربي)، والإرسال/الميكروفون على اليسار.
- الصفوف الداخلية (خيارات السؤال، الخطة، الأدوات) تستخدم `row-reverse` حتى تبدأ من اليمين دون عكس الأيقونات أو الحروف.

## قرارات التصميم

1. **محادثة فقط.** لا تبويبات، لا بطاقات إحصاء، لا لوحة تقدم. الاقتراحات الأربعة في الشاشة الفارغة كافية للبدء.
2. **الرد الغني جزء من الرسالة.** سؤال القدرات بطاقة قابلة للتوسعة، والخطة صفوف يومية صغيرة، وليست لوحة منفصلة.
3. **شريط كتابة واحد.** Plus يفتح رقائق أدوات: صورة سؤال، سؤال سريع، راجع أخطائي، محاكاة اختبار، خطة مذاكرة.
4. **Orb محلي.** مؤشر التفكير مرسوم بـ `View` و`Animated` داخل التطبيق، ولا يعتمد على أي موقع خارجي.
5. **اللوحة الآمنة ولوحة المفاتيح.** `SafeArea` للأعلى، حشو أسفل حقل الكتابة حسب الـ inset، و`KeyboardAvoidingView` على iOS، و`softwareKeyboardLayoutMode: resize` على Android.
6. **الحالات الخاصة ظاهرة بهدوء.** انقطاع الشبكة، خطأ الرد، رسالة بلا محتوى، نص طويل، وإعادة المحاولة من الرسالة أو الشريط العلوي.
7. **المحاكي يبقى محليًا.** `src/data/chat.ts` هو مصدر الرد حتى ربط الخادم. لا مفاتيح API في العميل.

## مراجع الإلهام

استُخدمت المواقع المرجعية كمبادئ فقط: هدوء الحركة، واجهة المحادثة، رقائق الأدوات، وبطاقات التوصية. لم يُنسخ كود ولا هوية بصرية، ولم تُضف اعتمادات ويب أو Tailwind.
